import GlobalStyle from 'style/globalStyle';
import StyledTheme from 'style/theme';

const App = () => {
  return (
    <StyledTheme>
      <GlobalStyle />
    </StyledTheme>
  );
};

export default App;
